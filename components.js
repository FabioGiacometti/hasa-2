import {  hero,
    clients,
    products,
    obras  } from "./main.js"

function imageComponent(dataset) {
//   for (let i = 0; i < dataset.length; i++) {
//     // var img = document.createElement("img");
//     // img.src = `dataset.url`;
//     // var src = document.getElementById("header");
//     // src.appendChild(img);}
//     console.log(dataset);
//   
console.log(dataset)
}

imageComponent(clients)

export { imageComponent }